import { MobileStub } from "../../components/mobile-stub";

/** Stub de Perfil (/m/perfil). La pantalla real llega en un delta. */
export default function MobilePerfil() {
  return <MobileStub title="Perfil" />;
}
