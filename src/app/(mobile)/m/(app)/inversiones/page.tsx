import { MobileStub } from "../../components/mobile-stub";

/** Stub de Inversiones (/m/inversiones). La pantalla real llega en un delta. */
export default function MobileInversiones() {
  return <MobileStub title="Inversiones" />;
}
