import { MobileStub } from "../../components/mobile-stub";

/** Stub de Patrimonio (/m/patrimonio). La pantalla real llega en un delta. */
export default function MobilePatrimonio() {
  return <MobileStub title="Patrimonio" />;
}
