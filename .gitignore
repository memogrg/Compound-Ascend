# dependencias
/node_modules
/.pnp
.pnp.*

# build
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# entorno — NUNCA commitear secretos
.env
.env.local
.env.development.local
.env.staging.local
.env.production.local
.env.sandbox.local
.env.prod.local

# logs
npm-debug.log*
yarn-debug.log*
*.log

# sistema
.DS_Store
.vercel
/coverage

# Claude Code (estado local)
.claude/scheduled_tasks.lock

# supabase (local CLI)
supabase/.temp
supabase/.branches

# Material de revisión (wireframes/docx) — no se versiona
/Revision_V2_BaseFinanciera/

# Material de revisión (spec Fase 1 Transacciones) — no se versiona
/Reestructuracion_Transacciones/
/test-results
/playwright-report
