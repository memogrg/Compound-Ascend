<?xml version="1.0" encoding="utf-8"?>
<!-- Widget "Indicador económico" — mismo look premium, robusto para RemoteViews: SIN
     <View>/<Space>/@font/vector/radial. El valor se pinta como Bitmap Space Mono en el provider;
     nombre y variación con fuentes del sistema; logo PNG; fondo <shape> lineal. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_bg"
    android:padding="18dp">

    <!-- Fila 1: etiqueta + logo (PNG) -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/tv_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"
            android:text="INDICADOR"
            android:fontFamily="sans-serif-medium"
            android:textSize="10sp"
            android:letterSpacing="0.14"
            android:textColor="@color/widget_muted" />

        <ImageView
            android:id="@+id/iv_logo"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:src="@drawable/logo_cartera"
            android:contentDescription="CARTERA+" />
    </RelativeLayout>

    <!-- Fila 2: nombre del indicador -->
    <TextView
        android:id="@+id/tv_ind_name"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text=""
        android:fontFamily="sans-serif-medium"
        android:textSize="14sp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textColor="@color/widget_text" />

    <!-- Fila 3: valor del indicador (bitmap Space Mono) -->
    <ImageView
        android:id="@+id/iv_amount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:adjustViewBounds="true"
        android:layout_marginTop="4dp"
        android:contentDescription="@null" />

    <!-- Fila 4: variación ▲/▼ % -->
    <TextView
        android:id="@+id/tv_change"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="7dp"
        android:text=""
        android:fontFamily="sans-serif-medium"
        android:textSize="12sp"
        android:maxLines="1"
        android:textColor="@color/widget_accent" />

    <!-- Fila 5: actualizado hh:mm -->
    <TextView
        android:id="@+id/tv_updated"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text=""
        android:fontFamily="sans-serif"
        android:textSize="9.5sp"
        android:textColor="@color/widget_muted" />
</LinearLayout>
