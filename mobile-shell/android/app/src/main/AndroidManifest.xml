<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link del retorno OAuth: Supabase redirige a
                 com.compoundascend.cartera://auth-callback?code=… tras el consentimiento
                 de Google en el navegador del sistema. @capacitor/app lo entrega a la
                 WebView vía el evento appUrlOpen (ver /m/login). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="com.compoundascend.cartera" android:host="auth-callback" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>

        <!-- Widget de pantalla de inicio "Patrimonio neto" (RemoteViews). exported=false:
             el repintado llega por intent EXPLÍCITO desde WidgetBridge y por el sistema. -->
        <receiver
            android:name=".WidgetPatrimonioProvider"
            android:label="@string/widget_patrimonio_label"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_patrimonio_info" />
        </receiver>

        <!-- Widget de pantalla de inicio "Presupuesto del mes" (RemoteViews). Mismo puente. -->
        <receiver
            android:name=".WidgetPresupuestoProvider"
            android:label="@string/widget_presupuesto_label"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_presupuesto_info" />
        </receiver>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Candado local con biometría (@aparajita/capacitor-biometric-auth → androidx.biometric).
         Se declara explícito además del merge del AAR, para BiometricPrompt (API 28+). -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
</manifest>
